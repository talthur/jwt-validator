apiVersion: v1
kind: Service
metadata:
  name: {{ include "jwt-validator.fullname" . }}
  labels:
    {{- include "jwt-validator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "jwt-validator.selectorLabels" . | nindent 4 }}
